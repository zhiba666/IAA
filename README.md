# 小小爆米花厂 · v0.1 正式版

原生 Canvas + JavaScript 轻经营流水线游戏，同一正式入口构建浏览器与抖音小游戏包。**v0.1 正式版**（包版本 `0.1.0`）包含六代工厂美术与分段自动化：亲手送入一盘货物 → 真实加工与出货 → 分别购买两段自动转运 → 调整产能、完成自动试运行 → 扩建。正式存档使用 schema 4；旧文档中的“1.5”是玩法方案名称。版本记录和本次检查结果见 [v0.1 发布说明](docs/releases/v0.1.md)。

## 接续开发入口

- [1.5 自动化实现与验证说明](docs/AUTOMATION_V15.md)：两段搬运、自动化、首代数值、迁移边界及可复跑经济模拟。
- [当前开发重心](docs/DEVELOPMENT_FOCUS.md)：当前版本范围与后续验证重点。
- [六代美术代码接入](docs/SIX_GEN_ART_INTEGRATION.md)：六代装配、资源预算及验收方法。
- [历史归档索引](archive/README.md)：旧方案、P0 原型、首代验收及制作记录，含原路径与 SHA-256 清单。

以当前 `src/`、`tests/` 和实际执行结果为准。旧截图、旧测试数量及历史美术验收不计为本轮新增结果。

## 运行与检查

需要 Node.js 18 或更新版本，无 npm 运行依赖。

```powershell
npm run build
npm start
npm test
npm run preflight
node tools/automation-balance.cjs
```

| 浏览器入口 | 玩法与存档 |
| --- | --- |
| [正式入口](http://127.0.0.1:4173/) | 默认 1.5 两段搬运与自动化；schema 4；兼容迁移已有 v2 工厂 |
| [v2 对照入口](http://127.0.0.1:4173/?mode=baseline) | 原有自动生产、原数值及独立 v2 存档 |
| [P0 历史实验](http://127.0.0.1:4173/?experiment=manual-transfer-p0) | 仅 A 段手动，首批 4 份、后续 12 份；独立 schema 3；不开扩建 |

本地服务器只监听本机。`npm test` 先构建源码；`preflight:strict` 提供更严格静态检查，不能替代真机验证。`build:dev` 仍禁用连点产量和模拟广告。

`tools/build.mjs` 生成 `web/game.bundle.js` 与 `build/douyin/`，调用美术构建脚本生成清单并复制冻结清单中的 84 个 PNG；两端检查尺寸和逐文件 SHA-256。源图、整屏参考图及未引用素材不随包发布。不要手改生成包或 `src/art-manifest.js`。本地 `config.local.json` 不被构建改写，格式见 [配置示例](config.local.example.json)。

## 正式玩法

三个工位和两段运输组成完整链路：

```text
爆锅 → 待装仓 → A 段搬运 → 装杯进料位 → 装杯
     → 待发仓 → B 段搬运 → 出货进料位 → 出货结算
```

按住货仓拖到对应进料口松手，或先点货仓、再点进料口。两种操作调用相同的核心命令；每段首次手动最多 4 份，此后整盘最多 24 份，实际份数还受库存和入口余量限制。取消、误投、双指、打开设置、缩放及切后台会释放预留，货物仍在源仓；搬运只进入下一工位的输入位，不能跳过加工或直接卖货。

| 购买 | 初期配置 | 收益 |
| --- | --- | --- |
| 设备改造 | 装杯 30 金币：2→3 份/秒；后续 90 金币到 4、180 金币到 6 | 提高该工位处理能力，整线是否提速另行预测 |
| 自动转运 | A 段 130 金币，B 段 330 金币，分别购买 | 永久省去对应搬运动作；已购能力随扩建保留 |
| 整盘与仓位 | 首次 60 金币：24→36 份 | 同步增加批量、两个入口及输出仓容量，减少补料频率 |

新开局爆锅/装杯/出货能力为 4/2/6 份每秒，两仓和两个入口各 24 份。自动运输按 120 Hz 核心时钟每 120 ticks 转运一批；后续批量与容量随物流等级和机器阶段增长。旧档使用明确的兼容规格，保留原升级能力。

第一扩建需要两段自动接通、自动试运行完成、累计真实销售 1200 份和 650 金币余额。试运行在最近 10 秒没有手动转运、两段自动已接通至少 10 秒后，连续 45 秒保持实测出货至少 3 份/秒，设备和运输能力也须达到这一门槛。达成里程碑永久保留；完成自动化后，前台持续不操作仍会生产。

当前配置集中在 [factory-rules.js](src/factory-rules.js)。[经济模拟](tools/automation-balance.cjs)的三条操作/购买路线首次销售均约 3.17 秒，两段接通分别在 3:08、4:01、4:14，首次扩建在 6:19、7:08、7:23。这些是确定性脚本结果，尚不代表真人完成时间；首段自动化的低频和非最优路线仍略晚于方案的 1～2 分钟候选区间。

## 生产、预测和交互边界

只有 `Game` 修改库存与金币，1 份真实售出结算 1 金币。守恒关系为：投产 = 售出 + 两个输出仓 + 两个输入位 + 三工位在制品；金币余额 + 累计支出 = 累计销售收入。预留货物仍占源仓空间，自动运输只取未预留库存。缺料等待，满仓保留完成批次，升级保留旧批次规格。

设备能力、自动运行基线、最近 10 秒实测出货分别展示。任意运输段尚未自动接通时，自动运行基线为 0；有限手动库存产生的销售继续计入实测。预测器使用隔离空工厂和真实生产规则，纳入运输周期、批量、入口、仓容、机器等级及旧档成长配置，提供设备/物流/自动化/扩建的前后预测。

点击工位查看设备改造，在“物流改造”分别购买自动化和整盘扩容。工位报价绑定工位、等级、价格和名称，购买后需要重新查看下一档。拖动松手不会再触发购买面板。主要触控区至少 44×44 逻辑像素，转运入口按更大的目标区域布局，并处理安全区与弹层优先级。

键盘 `1/2/3` 切换工位，`Enter` 提交当前确认报价，`M` 扩建、`S` 设置、`Escape` 关闭面板或取消搬运。触屏搬运已有点按等效操作，不依赖键盘。

## 存档与平台

正式存档键为 `little_popcorn_factory_automation_v4`。首次迁移时保留 `little_popcorn_factory_pipeline_v2` 原文，备份到 `little_popcorn_factory_automation_v4_backup_v2`，新档写入并回读一致后才切换运行工厂。失败时原工厂继续运行；迁移不会重新锁成手动、改低旧等级或销毁已有批次。正式模式不读取或删除 P0 和旧 `little_popcorn_factory_v1`。详细边界见 [1.5 存档迁移说明](docs/AUTOMATION_V15.md#存档迁移)。

后台暂停，返回继续，不按离开时长补产发钱。重开只写当前模式的存档键，写入失败保留当前工厂。浏览器用 WebAudio；抖音保留触摸、安全区、生命周期、存储与包内音效降级。广告接口不可用，不创建广告实例。

点击产量、火候惩罚、合同、分账、能量、广告、离线奖励、侧边栏等已退役系统保持停用。真人新手观察和抖音真机验收尚未完成；本次正式版本记录与 GitHub 发布不代表抖音平台上线。

## 文件职责

| 路径 | 职责 |
| --- | --- |
| `src/factory-rules.js`、`src/core.js` | 数值配置、两段转运、生产、成长、守恒与 schema 校验 |
| `src/production-insights.js` | 只读自动运行预测、运输/加工瓶颈及购买说明 |
| `src/six-generation-scene.js`、`src/first-generation-scene.js`、`src/art-layout.js` | 六代装配、托盘/入口/接通状态、统一坐标与命中区域 |
| `src/interface.js`、`src/renderer.js`、`src/production-scene.js` | HUD、工位与物流面板、命中映射及高代已有显示路径 |
| `src/main.js`、`src/platform.js`、`src/audio.js` | 正式入口、手势/报价、迁移写盘、生命周期与声音 |
| `src/art-assets.js`、`src/art-manifest.js`、`art-source/`、`assets/art/` | 双端图片加载、运行清单及美术源数据 |
| `tools/automation-balance.cjs` | 三种前期操作/购买策略、守恒、恢复及自动运行检查 |
| `tools/build.mjs`、`tools/art-build.mjs`、`tools/serve.mjs`、`tools/preflight.mjs` | 构建、资源复制、本地服务与静态预检 |
| `tests/` | 当前源码的核心、预测、输入、布局和平台回归 |

当前版本的构建、测试及 Chrome 检查见 [v0.1 发布说明](docs/releases/v0.1.md)。[1.5 执行结果](docs/AUTOMATION_V15.md#最终执行结果)与已有六代截图保留为对应阶段的历史证据。
