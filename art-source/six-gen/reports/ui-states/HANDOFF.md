# UI 状态美术验证板

已交付 14 个状态的一张总览 `ui-state-contact.png`，以及每状态 390×844、320×524 两种页面，共 28 张页面 PNG。每张均有 SVG 分层源和 JSON 证据；状态语义、条件、动态字段及可达性限制在 `states.json`。

覆盖：金币不足、锁定、满级、报价过期、二次点击处理中；试运行前、连续合格累计、中断、通过；单瓶颈、共同限制、观察中；六代厂房建成与设备全满配两个不同终局。没有领奖、第七代、转生入口。

仅引用最终 `integration/exports` 的既有无字底板和 UI 图标，包括自动化、代际、完成三枚新标志。底板使用九宫格组合，图标等比缩放；所有文字、徽记上的 6 和进度条均在预览 SVG 中独立叠加，输入 PNG 未写入文字、金额或价格。已在共享禁用按钮恢复原 alpha 后重新加载全部最终资源并生成证据，SHA 与最终清单一致。

已目检全部总览状态及代表性双视口页面，修复顶部文字对比。29 PNG 尺寸/签名/SHA、引用资产 SHA、保守文字范围估算和终局禁用入口文字检查通过，详见 `qa-report.json`。每个主按钮显示高度至少 44 px，但本包没有进行实际点击或命中测试。

这些是 synthetic-art-only 状态表达示例：进度条示意值不代表游戏数据；条件说明不证明运行可达；交易、存档、运行回归和真机验证均为 NOT_RUN。未新增生图、未修改正式代码或输入资产。

重现：`node art-source/six-gen/reports/ui-states/render-ui-states.cjs`；核验：`node art-source/six-gen/reports/ui-states/verify-ui-states.cjs`。两脚本仅写本目录。
