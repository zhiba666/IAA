# A4｜物流与仓位 — 独立委派卡

本卡是待执行任务，不是完成报告。

## 目标
大仓扩展、桥接、空托盘、两个入口圈；共享产品填充和容量状态

## 唯一允许写入路径
`art-source/six-gen/logistics/**`

## 共同约束（每次委派都必须携带）
先读总计划 parts/02、03、06、07、09；以 Gate 0 的 contract/reference lock 为准。计划基线与当前 HEAD 不同时报告差异，不回退仓库。默认只交付阶段 A。

只写下面列出的本包路径；其他目录全部只读。禁止覆盖正式 src/assets/build、根级 AGENTS.md、旧批次源图/清单和其他 agent 文件。不运行会回写全局清单的旧导出脚本；本包导出脚本只能写本包。

先复用再生成；有合格既有件不能复制一份改名充数。没有实际可见参考不得声称看过。生图工具不可用则提交 generation-job/BLOCKED_GENERATION；占位图只用于标注的灰盒，不能算成图。不能自行启用付费API或假设子agent继承所有图像工具。

生产RGBA独立件，保留原图/实际提示词/参考SHA/导出配方，不烘焙中文、数字、价格和产品。等比缩放、各rig坐标独立，input/output/clip/slots/hitPolygon明确。空机壳不含工作头，活动工作头按实际lanes及各job进度；无料不假忙，旧批次不复制。

交付 HANDOFF.md：实际文件和数量、复用列表、生成/导出/装配分别状态、依赖的锁定SHA、真正执行的检查及证据、未执行检查、阻塞/缺陷、下一依赖、禁止路径检查。没有运行的测试写 NOT_RUN。

## 专项要求
9候选：buffer_bulk_large_back/front、buffer_cups_rack_back/front、conveyor_bridge_back/front、transfer_tray_empty、input_cup_collar、input_ship_collar。分别测两仓空/仅1份/半满/满仓；填充数不能只读generation。保持A、B两条合法搬运路径，容器不是新增库存实体。大仓前/后层与旧产品可组合，入口触控面与视觉圈解耦。
