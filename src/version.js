'use strict';

// Application release version; save schemas and art contracts are independent.
// The build checks this value against package.json before producing either package.
const APP_VERSION = '1.1.0';

module.exports = { APP_VERSION };
